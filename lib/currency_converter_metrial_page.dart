import 'package:flutter/material.dart';

class CurrencyConverterMetrialPage extends StatelessWidget {
  const CurrencyConverterMetrialPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Column(children: [Text("Hello World!!!!")])),
    );
  }
}
